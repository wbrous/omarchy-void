#!/bin/bash

# omarchy:summary=Returns true if the void-packages repository is up and available.

curl -sf --connect-timeout 30 --retry 3 --retry-delay 3 -A "omarchy-update" \
  "https://github.com/void-linux/void-packages" >/dev/null
