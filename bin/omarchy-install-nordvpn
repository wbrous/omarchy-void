#!/bin/bash

# omarchy:summary=Install the NordVPN service with optional GUI.
# omarchy:requires-sudo=true

echo "NordVPN is not available in the official Void Linux repositories."
echo "You can build it from source using xbps-src if needed."
exit 1
